<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>All Students</title>
</head>
<body>
    <nav class="mb-5 sticky-top d-flex flex-wrap align-items-center justify-content-around p-3" style="background-color: rgb(68, 68, 70);">
        <div class="d-flex flex-column">
            <h1 class="text-light">Find a Student</h1>
        </div>
        <div class="d-flex flex-column">
            <form class="d-flex  flex-row mt-3" action="/admin/search" method="POST">
                <input class="form-control" name="search" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-secondary my-sm-0" type="submit">Search</button>
            </form>
            <a href="/admin/home" class="btn btn-secondary text-light align-items-center mt-3">Back</a>
        </div>
    </nav>

    <% if(students.length === 0){ %> 
        <div class="container  d-flex  align-items-center justify-content-center">
            <div class="bg-secondary d-flex rounded p-3 mt-5 text-light">
                <h2>Any student now add a student!</h2>
            </div>
        </div>
    <% }else{ %>
        <% students.forEach(student => { %>   
            <div class="container">
                <form  method="POST" action="/admin/<%=student.id %>?_method=DELETE" class="d-flex flex-column bg-secondary mb-3 p-3 justify-content-around"  style="box-shadow: 3px 3px 3px rgba(0, 0, 0, 0.705);">
                    <div class="d-flex flex-row justify-content-around mb-3 mt-3" >
                        <div class="d-flex flex-column justify-content-center"> 
                            <h4 style="color: black;">Name: <%= student.name %> </h4>
                            <h5 class="text-light">CPF: <%= student.cpf %></h5>
                        </div>
                        <div class="d-flex flex-column justify-content-center ">
                            <h4 style="color: black;">Class Number: <%= student.classNumber %></h4>
                            <h5 class="text-light">Phone: <%= student.phone %></h4></h5>
                        </div>
                    </div>
                    <div class="container d-flex flex-row  justify-content-around  align-items-center">
                        <a class="btn btn-info" style="font-size: 25px; " href="/admin/gradesmenu/<%= student.id %>">Edit Grade</a>
                        <button class="btn btn-danger " style="font-size: 25px; " type="submit" >Delete</button>
                    </div>
                </form>
            </div>
        <% }) %> 

    <%}%>  
    <h3 class="text-center mt-3 mb-3">Developed by <a class="text-secondary " style="text-decoration:none;" href="https://github.com/AugustoBernardes" >Augusto</a></h3>
</body>
</html>